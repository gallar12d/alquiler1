@extends('layouts.app')@section('content')<div class="row">    <div class ="col-md-10 col-md-offset-1">        <h3>Listado de sedes</h3>        @if(count($sedes))          <table class="table table-striped datatable">            <thead>                <tr>                    <th>Nombre</th>                    <th>Acciones</th>                                   </tr>            </thead>            <tbody>                @foreach($sedes as $sede)                <tr>                    <td>{{$sede->nombre}}</td>                    <td><a href="{{url('sede/delete/'.$sede->id)}}">Eliminar</a><br>                        <a href="{{url('sede/edit/'.$sede->id)}}">Editar</a></td>                                    </tr>                @endforeach                            </tbody>        </table>                @endif                 <h3>Crear nueva sede</h3>        <form class="formsubmit" method="post" action="{{ url('/sede/create') }}">            <input type="hidden" name="_token" value="{{ csrf_token() }}">            <div class="form-group">                <label for="comment">Nombre sede</label>                   <input  required type="text" class="form-control" name ="nombre" >            </div>            <button type="submit" class="btn btn-default">Crear</button>        </form>    </div></div>@endsection